import {InputProcessor} from "./InputProcessor.js";

class KeyboardProcessor extends InputProcessor {
    constructor(context,player,jumpButton,dashButton) {
        super(context,player,jumpButton,dashButton);

    }
}